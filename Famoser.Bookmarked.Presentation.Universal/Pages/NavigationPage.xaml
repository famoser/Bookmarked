<Page
    x:Class="Famoser.Bookmarked.Presentation.Universal.Pages.NavigationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:navigationPage="using:Famoser.Bookmarked.Presentation.Universal.UserControls.NavigationPage"
    mc:Ignorable="d" Loaded="Page_Loaded"
    DataContext="{Binding NavigationViewModel, Source={StaticResource Locator}}">

    <Page.Resources>
        <DataTemplate x:Key="FolderDataTemplate">
            <StackPanel Style="{StaticResource ListViewDataTemplateStackPanel}" AllowDrop="True" DragOver="UIElement_OnDragOver" Drop="UIElement_OnDrop">
                <TextBlock Style="{StaticResource ItemHeader}" Text="{Binding Name}" />
                <TextBlock Style="{StaticResource ItemDescription}" Text="{Binding Description}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="GarbageFolderDataTemplate">
            <Grid Style="{StaticResource ListViewDataTemplateGrid}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Style="{StaticResource ItemHeader}" Text="{Binding Name}" />
                    <TextBlock Style="{StaticResource ItemDescription}" Text="{Binding Description}" />
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <Button Style="{StaticResource PrimaryButton}" Command="{Binding RecoverFolderCommand}" CommandParameter="{Binding }">
                        <Button.Content>
                            <SymbolIcon Symbol="Accept" />
                        </Button.Content>
                    </Button>
                    <Button Style="{StaticResource SecondaryButton}" Command="{Binding RemoveFolderCommand}" CommandParameter="{Binding }">
                        <Button.Content>
                            <SymbolIcon Symbol="Delete" />
                        </Button.Content>
                    </Button>
                </StackPanel>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="EntryDataTemplate">
            <Grid Style="{StaticResource ListViewDataTemplateGrid}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Stretch="Uniform" Grid.Column="1" Width="80">
                    <Image.Source>
                        <BitmapImage UriSource="{Binding IconUri}" />
                    </Image.Source>
                </Image>
                <StackPanel Grid.Column="3">
                    <TextBlock Style="{StaticResource ItemHeader}" Text="{Binding Name}" />
                    <TextBlock Style="{StaticResource ItemDescription}" Text="{Binding Description}" />
                </StackPanel>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="ContentTypeModelTemplate">
            <TextBlock Text="{Binding Name}" Style="{StaticResource ParagraphTextBlock}" />
        </DataTemplate>
    </Page.Resources>

    <Grid Style="{StaticResource LayoutGrid}">
        <Grid Style="{StaticResource FullContentGrid}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <navigationPage:FolderHeader DataContext="{Binding }" Visibility="{Binding InSearchMode, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"/>
                <navigationPage:SearchHeader DataContext="{Binding }" Visibility="{Binding InSearchMode, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <Grid AllowDrop="True" x:Name="MoveUpGrid" Visibility="Collapsed" Drop="MoveUpGrid_OnDrop" DragOver="MoveUpGrid_OnDragOver" Background="{ThemeResource SystemControlBackgroundAccentBrush}" Width="70">
                    <SymbolIcon Symbol="Up" Foreground="White" />
                </Grid>
                <ProgressBar VerticalAlignment="Top" Value="{Binding ProgressViewModel.ActiveProgress, Source={StaticResource Locator}, Mode=TwoWay}" Maximum="{Binding ProgressViewModel.MaxProgress, Source={StaticResource Locator}}" Visibility="{Binding ProgressViewModel.ShowProgress, Source={StaticResource Locator}, Converter={StaticResource BoolToVisibilityConverter}}" Minimum="0" />
            </Grid>
            <ScrollViewer Grid.Row="1" Style="{StaticResource ContentScrollViewer}">
                <StackPanel>
                    <StackPanel Visibility="{Binding NavigationViewMode, Converter={StaticResource EnumVisibilityConverter}, ConverterParameter=Default}">
                        <ListView Style="{StaticResource DropListView}" ItemsSource="{Binding SelectedFolder.Folders}" ItemTemplate="{StaticResource FolderDataTemplate}" Visibility="{Binding SelectedFolder.Folders, Converter={StaticResource CollectionToVisibilityConverter}}" DragItemsStarting="ListView_OnDragItemsStarting" DragItemsCompleted="ListView_DragItemsCompleted" ItemClick="ListView_ItemClick"  />
                        <ListView Style="{StaticResource DragListView}" ItemsSource="{Binding SelectedFolder.Entries}" ItemTemplate="{StaticResource EntryDataTemplate}" DragItemsStarting="ListView_OnDragItemsStarting" DragItemsCompleted="ListView_DragItemsCompleted" ItemClick="ListView_ItemClick"    />
                    </StackPanel>
                    <StackPanel Visibility="{Binding NavigationViewMode, Converter={StaticResource EnumVisibilityConverter}, ConverterParameter=Search}">
                        <ListView Style="{StaticResource ClickListView}" ItemsSource="{Binding SearchFolders}" ItemTemplate="{StaticResource FolderDataTemplate}" Visibility="{Binding SearchFolders, Converter={StaticResource CollectionToVisibilityConverter}}" ItemClick="ListView_ItemClick" />
                        <ListView Style="{StaticResource ClickListView}" ItemsSource="{Binding SearchEntries}" ItemTemplate="{StaticResource EntryDataTemplate}" ItemClick="ListView_ItemClick" />
                    </StackPanel>
                    <StackPanel Visibility="{Binding NavigationViewMode, Converter={StaticResource EnumVisibilityConverter}, ConverterParameter=Garbage}">
                        <ListView Style="{StaticResource FullListView}" ItemsSource="{Binding GarbageFolder.Folders}" ItemTemplate="{StaticResource GarbageFolderDataTemplate}" Visibility="{Binding GarbageFolder.Folders, Converter={StaticResource CollectionToVisibilityConverter}}" />
                        <ListView Style="{StaticResource FullListView}" ItemsSource="{Binding GarbageFolder.Entries}" ItemTemplate="{StaticResource EntryDataTemplate}"    />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>


    <Page.BottomAppBar>
        <CommandBar >
            <CommandBar.PrimaryCommands>
                <AppBarToggleButton IsChecked="{Binding InGarbageMode, Mode=TwoWay}" Icon="Delete" Label="Garbage" />
                <AppBarToggleButton IsChecked="{Binding InSearchMode, Mode=TwoWay}" Icon="Find" Label="Find" />
                <AppBarButton Command="{Binding RefreshCommand}" Icon="Refresh" Label="Refresh" />
                <AppBarButton Icon="Add" Label="New Entry" >
                    <AppBarButton.Flyout>
                        <Flyout Placement="Bottom">
                            <StackPanel>
                                <Button Content="add Folder" Command="{Binding AddFolderCommand}"></Button>
                                <ListView 
                                    ItemsSource="{Binding ContentTypeModels}" 
                                    ItemTemplate="{StaticResource ContentTypeModelTemplate}"
                                    ItemContainerStyle="{StaticResource ClickListView}" 
                                    ItemClick="ListView_ItemClick"/>
                            </StackPanel>
                        </Flyout>
                    </AppBarButton.Flyout>
                </AppBarButton>
            </CommandBar.PrimaryCommands>
            <CommandBar.SecondaryCommands>
                <AppBarButton Command="{Binding HelpCommand}" Icon="Help" Label="About" />
                <AppBarButton Command="{Binding EditFolderCommand}" CommandParameter="{Binding SelectedFolder}" Icon="Edit" Label="Edit Folder" />
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
