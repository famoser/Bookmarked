<Page
    x:Class="Famoser.Bookmarked.Presentation.Universal.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Famoser.Bookmarked.Presentation.Universal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding LoginViewModel, Source={StaticResource Locator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" VerticalAlignment="Center">
            <StackPanel >
                <TextBlock Visibility="{Binding PasswordUnlockFailed, Converter={StaticResource BoolToVisibilityConverter}}" Text="Das Password ist falsch" Style="{StaticResource BigTexBlock}" />
                <TextBlock Visibility="{Binding IsFirstTime, Converter={StaticResource BoolToVisibilityConverter}}" Text="Willkommen! Wähle dein Passwort" Style="{StaticResource BigTexBlock}" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <StackPanel>
                <TextBlock Text="Passwort" Style="{StaticResource SecondaryBigTexBlock}" />
                <PasswordBox Password="{Binding Password, Mode=TwoWay}" Style="{StaticResource PasswordBox}" />
                <Button HorizontalAlignment="Center" Padding="50,10" Style="{StaticResource FlatButton}" Content="Login" Command="{Binding LoginCommand}"></Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
