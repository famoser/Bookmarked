<Page
    x:Class="Famoser.Bookmarked.Presentation.Universal.Pages.InfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Famoser.Bookmarked.Presentation.Universal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding ImportViewModel, Source={StaticResource Locator}}">

    <Page.Resources>
        <DataTemplate x:Key="ApiTraceDataTemplate">
            <TextBlock Style="{StaticResource TraceTextBlock}" Text="{Binding FullDescription}" />
        </DataTemplate>
    </Page.Resources>

    <Grid Style="{StaticResource LayoutGrid}">
        <Grid Style="{StaticResource SmallContentGrid}">
            <StackPanel>
                <StackPanel Style="{StaticResource HeaderStackPanel}">
                    <TextBlock x:Uid="InfoPageTitle" Style="{StaticResource H1TextBlock}" Text="{Binding Source={StaticResource Localized}, Path=[InfoPageTitle.Text]}" />
                    <Border Style="{StaticResource HeaderBottomBorder}" />
                    <TextBlock x:Uid="InfoPageDescription" Style="{StaticResource H2TextBlock}" />
                </StackPanel>

                <TextBlock x:Uid="InfoPageQATitle" Style="{StaticResource H2TextBlock}" />
                <TextBlock x:Uid="InfoPageAboutApplicationQuestion" Style="{StaticResource QuestionTextBlock}" />
                <TextBlock x:Uid="InfoPageAboutApplicationAnswer" Style="{StaticResource AnswerTextBlock}" />
                <TextBlock x:Uid="InfoPageAboutSecurityQuestion" Style="{StaticResource QuestionTextBlock}" />
                <TextBlock x:Uid="InfoPageAboutSecurityAnswer" Style="{StaticResource AnswerTextBlock}" />
                <TextBlock x:Uid="InfoPageAboutEncryptionQuestion" Style="{StaticResource QuestionTextBlock}" />
                <TextBlock x:Uid="InfoPageAboutEncryptionAnswer" Style="{StaticResource AnswerTextBlock}" />
                <TextBlock x:Uid="InfoPageAboutPasswordQuestion" Style="{StaticResource QuestionTextBlock}" />
                <TextBlock x:Uid="InfoPageAboutPasswordAnswer" Style="{StaticResource AnswerTextBlock}" />
                
                
                <TextBlock x:Uid="InfoPageSyncLogTitle" Style="{StaticResource H2TextBlock}" />
                <ListView ItemContainerStyle="{StaticResource SmallStretchListViewItemStyle}" ItemTemplate="{StaticResource ApiTraceDataTemplate}" ItemsSource="{Binding ApiViewModel.SyncActionInformation, Source={StaticResource Locator}}" />


                <TextBlock Text="Export / Import credentials (fully encrypted)" Style="{StaticResource SecondaryBigTexBlock}" />
                <Grid  Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" HorizontalAlignment="Stretch" Content="Export" Command="{Binding ExportCredentialsCommand}" />
                    <Button Grid.Column="2" HorizontalAlignment="Stretch"  Content="Import" Command="{Binding ImportCredentialsCommand}" />
                </Grid>

                <TextBlock Text="Danger functions" Style="{StaticResource SecondaryBigTexBlock}" />
                <Grid  Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Button Grid.Column="0" HorizontalAlignment="Stretch" Content="Reset Installation" Command="{Binding ResetInstallationCommand}" />
                    <Button Grid.Column="2" HorizontalAlignment="Stretch" Content="Reset Application" Command="{Binding ResetApplicationCommand}" />
                </Grid>
                <StackPanel Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <TextBlock FontFamily="Segoe UI" Margin="10" Foreground="White">
                        <Run Text="made with &#x2661; by" />
                        <Hyperlink NavigateUri="http://famoser.ch">
                            <Run Text="famoser"/>
                        </Hyperlink>
                        <LineBreak />
                        <Run Text="source code on" />
                        <Hyperlink NavigateUri="https://github.com/famoser/Bookmarked" Foreground="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                            <Run Text="github"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
