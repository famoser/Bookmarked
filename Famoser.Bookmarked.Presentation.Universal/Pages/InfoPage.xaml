<Page
    x:Class="Famoser.Bookmarked.Presentation.Universal.Pages.InfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Famoser.Bookmarked.Presentation.Universal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding ImportViewModel, Source={StaticResource Locator}}">

    <Page.Resources>
        <DataTemplate x:Key="ApiTraceDataTemplate">
            <TextBlock Style="{StaticResource TraceTextBlock}" Text="{Binding FullDescription}" />
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer>
            <StackPanel>
                <TextBlock Text="Help" Style="{StaticResource BigTexBlock}" />
                <TextBlock Text="What is this application" Style="{StaticResource SecondaryBigTexBlock}" />
                <TextBlock Text="This application provides a convenient way to store bookmarks and other simple things you want to remember. All entries are synced accross your devices, and are accessible with a password of your choosing" Style="{StaticResource ValueTextBlock}" />
                <TextBlock Text="How secure is this" Style="{StaticResource SecondaryBigTexBlock}" />
                <TextBlock Text="All connection is encrypted by SSL (the same way webpages do their encryption), the special fields of the entries are additionally encrypted by the password you chose" Style="{StaticResource ValueTextBlock}" />

                <TextBlock Text="Synchronization log" Style="{StaticResource SecondaryBigTexBlock}" />
                <ListView ItemContainerStyle="{StaticResource SmallStretchListViewItemStyle}" ItemTemplate="{StaticResource ApiTraceDataTemplate}" ItemsSource="{Binding ApiViewModel.SyncActionInformations, Source={StaticResource Locator}}" />

                <TextBlock Text="Export / Import data (fully encrypted)" Style="{StaticResource SecondaryBigTexBlock}" />
                <Grid  Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" HorizontalAlignment="Stretch" Content="Export" Command="{Binding ExportCommand}" />
                    <Button Grid.Column="2" HorizontalAlignment="Stretch"  Content="Import" Command="{Binding ImportCommand}" />
                </Grid>

                <TextBlock Text="Export / Import credentials (fully encrypted)" Style="{StaticResource SecondaryBigTexBlock}" />
                <Grid  Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" HorizontalAlignment="Stretch" Content="Export" Command="{Binding ExportCredentialsCommand}" />
                    <Button Grid.Column="2" HorizontalAlignment="Stretch"  Content="Import" Command="{Binding ImportCredentialsCommand}" />
                </Grid>

                <TextBlock Text="Danger functions" Style="{StaticResource SecondaryBigTexBlock}" />
                <Grid  Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Button Grid.Column="0" HorizontalAlignment="Stretch" Content="Reset Application" Command="{Binding ResetApplicationCommand}" />
                </Grid>
                <StackPanel  Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <TextBlock FontFamily="Segoe UI" Margin="10" Foreground="White">
                        <Run Text="made with &#x2661; by" />
                        <Hyperlink NavigateUri="http://famoser.ch">
                            <Run Text="famoser"/>
                        </Hyperlink>
                        <LineBreak />
                        <Run Text="source code on" />
                        <Hyperlink NavigateUri="https://github.com/famoser/Study" Foreground="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                            <Run Text="github"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
