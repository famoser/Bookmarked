<Page
    x:Class="Famoser.Bookmarked.Presentation.Universal.Pages.InfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Famoser.Bookmarked.Presentation.Universal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding ImportViewModel, Source={StaticResource Locator}}">

    <Page.Resources>
        <DataTemplate x:Key="ApiTraceDataTemplate">
            <TextBlock Style="{StaticResource TraceTextBlock}" Text="{Binding FullDescription}" />
        </DataTemplate>
    </Page.Resources>

    <Grid Style="{StaticResource LayoutGrid}">
        <Grid Style="{StaticResource SmallContentGrid}">
            <StackPanel>
                <StackPanel Style="{StaticResource HeaderStackPanel}">
                    <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageTitle]}" Style="{StaticResource H1TextBlock}"  />
                    <Border Style="{StaticResource HeaderBottomBorder}" />
                    <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageDescription]}" Style="{StaticResource H2TextBlock}" />
                </StackPanel>

                <!-- Q&A -->
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageQATitle]}" Style="{StaticResource H2TextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageQADescription]}" Style="{StaticResource ParagraphTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutApplicationQuestion]}" Style="{StaticResource QuestionTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutApplicationAnswer]}" Style="{StaticResource AnswerTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutSecurityQuestion]}" Style="{StaticResource QuestionTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutSecurityAnswer]}" Style="{StaticResource AnswerTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutEncryptionQuestion]}" Style="{StaticResource QuestionTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutEncryptionAnswer]}" Style="{StaticResource AnswerTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutPasswordQuestion]}" Style="{StaticResource QuestionTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutPasswordAnswer]}" Style="{StaticResource AnswerTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutAlgorithmQuestion]}" Style="{StaticResource QuestionTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageAboutAlgorithmAnswer]}" Style="{StaticResource AnswerTextBlock}" />
                
                <!-- sync log -->
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageSyncLogTitle]}" Style="{StaticResource H2TextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageSyncLogDescription]}" Style="{StaticResource ParagraphTextBlock}" />
                <ListView Style="{StaticResource SmallListView}" ItemTemplate="{StaticResource ApiTraceDataTemplate}" ItemsSource="{Binding ApiViewModel.SyncActionInformation, Source={StaticResource Locator}}" />

                <!-- import/export data -->
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageDataExchangeTitle]}" Style="{StaticResource H2TextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageImportDataDescription]}" Style="{StaticResource ParagraphTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageExportDataDescription]}" Style="{StaticResource ParagraphTextBlock}" />

                <!-- import/export credentials -->
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageExportImportCredentialsTitle]}" Style="{StaticResource H2TextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageImportCredentialsDescription]}" Style="{StaticResource ParagraphTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageExportCredentialsDescription]}" Style="{StaticResource ParagraphTextBlock}" />


                <!-- danger functions -->
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageDangerFunctionsTitle]}" Style="{StaticResource H2TextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageImportCredentialsDescription]}" Style="{StaticResource ParagraphTextBlock}" />
                <TextBlock Text="{Binding Source={StaticResource Localized}, Path=[InfoPageExportCredentialsDescription]}" Style="{StaticResource ParagraphTextBlock}" />


                <TextBlock Text="Export / Import credentials (fully encrypted)" Style="{StaticResource SecondaryBigTexBlock}" />
                <Grid  Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" HorizontalAlignment="Stretch" Content="Export" Command="{Binding ExportCredentialsCommand}" />
                    <Button Grid.Column="2" HorizontalAlignment="Stretch"  Content="Import" Command="{Binding ImportCredentialsCommand}" />
                </Grid>

                <TextBlock Text="Danger functions" Style="{StaticResource SecondaryBigTexBlock}" />
                <Grid  Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Button Grid.Column="0" HorizontalAlignment="Stretch" Content="Reset Installation" Command="{Binding ResetInstallationCommand}" />
                    <Button Grid.Column="2" HorizontalAlignment="Stretch" Content="Reset Application" Command="{Binding ResetApplicationCommand}" />
                </Grid>
                <StackPanel Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <TextBlock FontFamily="Segoe UI" Margin="10" Foreground="White">
                        <Run Text="made with &#x2661; by" />
                        <Hyperlink NavigateUri="http://famoser.ch">
                            <Run Text="famoser"/>
                        </Hyperlink>
                        <LineBreak />
                        <Run Text="source code on" />
                        <Hyperlink NavigateUri="https://github.com/famoser/Bookmarked" Foreground="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                            <Run Text="github"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
