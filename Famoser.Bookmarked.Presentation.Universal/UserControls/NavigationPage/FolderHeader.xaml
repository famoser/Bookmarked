<UserControl
    x:Class="Famoser.Bookmarked.Presentation.Universal.UserControls.NavigationPage.FolderHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Famoser.Bookmarked.Presentation.Universal.UserControls.NavigationPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{Binding NavigationViewModel, Source={StaticResource Locator}}">

    <StackPanel Style="{StaticResource HeaderStackPanel}">
        <TextBlock Text="{Binding SelectedFolder.Name}" Style="{StaticResource H1TextBlock}" />
        <Grid>
            <TextBlock Text="{Binding SelectedFolder.Description}" Style="{StaticResource H1DescriptionTextBlock}" />
            <TextBlock VerticalAlignment="Bottom" HorizontalAlignment="Right" Style="{StaticResource H1DescriptionTextBlock}">
                    <Run Text="{Binding TotalEntries}" />
                    <Run Text="entries" />
                    <Run Text="{Binding TotalFolders}" />
                    <Run Text="folders" />
            </TextBlock>
        </Grid>
        <Border Style="{StaticResource HeaderBottomBorder}" />
    </StackPanel>
</UserControl>
