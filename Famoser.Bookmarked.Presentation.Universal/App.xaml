<Application
    x:Class="Famoser.Bookmarked.Presentation.Universal.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:visibility="using:Famoser.FrameworkEssentials.UniversalWindows.Converters.Visibility"
    xmlns:viewModels="using:Famoser.Bookmarked.Presentation.Universal.ViewModels"
    xmlns:converters="using:Famoser.Bookmarked.Presentation.Universal.Converters"
    xmlns:strings="using:Famoser.Bookmarked.Presentation.Universal.Strings"
    RequestedTheme="Light">
    <Application.Resources>
        <ResourceDictionary>
            <viewModels:ViewModelLocator x:Key="Locator" />
            <strings:LocalizedStrings x:Key="Localized"/>
            
            <visibility:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
            <visibility:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:UriToStringConverter x:Key="UriToStringConverter" />

            <!-- controls -->
            <Style x:Key="StretchListViewItemStyle" TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="0,0,0,1" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                            <!--<ContentPresenter />-->
                            <ListViewItemPresenter CheckBrush="{ThemeResource SystemControlForegroundBaseMediumHighBrush}" ContentMargin="{TemplateBinding Padding}" CheckMode="Inline" ContentTransitions="{TemplateBinding ContentTransitions}" CheckBoxBrush="{ThemeResource SystemControlForegroundBaseMediumHighBrush}" DragForeground="{ThemeResource ListViewItemDragForegroundThemeBrush}" DragOpacity="{ThemeResource ListViewItemDragThemeOpacity}" DragBackground="{ThemeResource ListViewItemDragBackgroundThemeBrush}" DisabledOpacity="{ThemeResource ListViewItemDisabledThemeOpacity}" FocusBorderBrush="{ThemeResource SystemControlForegroundAltHighBrush}" FocusSecondaryBorderBrush="{ThemeResource SystemControlForegroundBaseHighBrush}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" PointerOverForeground="{ThemeResource SystemControlHighlightAltBaseHighBrush}" PressedBackground="{ThemeResource SystemControlHighlightListMediumBrush}" PlaceholderBackground="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" PointerOverBackground="{ThemeResource SystemControlHighlightListLowBrush}" ReorderHintOffset="{ThemeResource ListViewItemReorderHintThemeOffset}" SelectedPressedBackground="{ThemeResource SystemControlHighlightListAccentHighBrush}" SelectionCheckMarkVisualEnabled="True" SelectedForeground="{ThemeResource SystemControlHighlightAltBaseHighBrush}" SelectedPointerOverBackground="{ThemeResource SystemControlHighlightListAccentMediumBrush}" SelectedBackground="{ThemeResource SystemControlHighlightListAccentLowBrush}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ListViewDataTemplateStackPanel" TargetType="StackPanel">
                <Setter Property="Background" Value="{ThemeResource SystemAccentColor}" />
            </Style>

            <Style x:Key="ListViewDataTemplateGrid" TargetType="Grid">
                <Setter Property="Background" Value="{ThemeResource SystemAccentColor}" />
            </Style>


            <Style x:Key="SmallStretchListViewItemStyle" TargetType="ListViewItem">
                <Setter Property="MinHeight" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="0,2" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                            <!--<ContentPresenter />-->
                            <ListViewItemPresenter CheckBrush="{ThemeResource SystemControlForegroundBaseMediumHighBrush}" ContentMargin="{TemplateBinding Padding}" CheckMode="Inline" ContentTransitions="{TemplateBinding ContentTransitions}" CheckBoxBrush="{ThemeResource SystemControlForegroundBaseMediumHighBrush}" DragForeground="{ThemeResource ListViewItemDragForegroundThemeBrush}" DragOpacity="{ThemeResource ListViewItemDragThemeOpacity}" DragBackground="{ThemeResource ListViewItemDragBackgroundThemeBrush}" DisabledOpacity="{ThemeResource ListViewItemDisabledThemeOpacity}" FocusBorderBrush="{ThemeResource SystemControlForegroundAltHighBrush}" FocusSecondaryBorderBrush="{ThemeResource SystemControlForegroundBaseHighBrush}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" PointerOverForeground="{ThemeResource SystemControlHighlightAltBaseHighBrush}" PressedBackground="{ThemeResource SystemControlHighlightListMediumBrush}" PlaceholderBackground="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" PointerOverBackground="{ThemeResource SystemControlHighlightListLowBrush}" ReorderHintOffset="{ThemeResource ListViewItemReorderHintThemeOffset}" SelectedPressedBackground="{ThemeResource SystemControlHighlightListAccentHighBrush}" SelectionCheckMarkVisualEnabled="True" SelectedForeground="{ThemeResource SystemControlHighlightAltBaseHighBrush}" SelectedPointerOverBackground="{ThemeResource SystemControlHighlightListAccentMediumBrush}" SelectedBackground="{ThemeResource SystemControlHighlightListAccentLowBrush}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ContentScrollViewer" TargetType="ScrollViewer">
                <Setter Property="HorizontalScrollMode" Value="Disabled" />
                <Setter Property="VerticalScrollBarVisibility" Value="Hidden" />
            </Style>

            <Style x:Key="HeaderStackPanel" TargetType="StackPanel">
                <Setter Property="Background" Value="#FAFAFA" />
            </Style>

            <Style x:Key="LayoutGrid" TargetType="Grid">
                <Setter Property="Margin" Value="10" />
            </Style>

            <Style x:Key="FullContentGrid" TargetType="Grid">

            </Style>

            <Style x:Key="SmallContentGrid" TargetType="Grid">
                <Setter Property="MaxWidth" Value="450" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
            </Style>

            <Style x:Key="HeaderBottomBorder" TargetType="Border" BasedOn="{StaticResource BasicElement}">
                <Setter Property="BorderThickness" Value="0,8,0,0" />
                <Setter Property="BorderBrush" Value="{ThemeResource SystemAccentColor}" />
                <Setter Property="Width" Value="21" />
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="Margin" Value="10,0" />
            </Style>

            <!-- Basic elements -->
            <Style x:Key="BasicElement" TargetType="FrameworkElement">
                <Setter Property="Margin" Value="0" />
            </Style>

            <Style x:Key="BasicTextBlock" TargetType="TextBlock" BasedOn="{StaticResource BasicElement}">
                <Setter Property="Padding" Value="10" />
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="BasicControl" TargetType="Control" BasedOn="{StaticResource BasicElement}">
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="FontSize" Value="21" />
                <Setter Property="Padding" Value="10" />
            </Style>

            <!-- text fields -->
            <Style x:Key="ParagraphTextBlock" TargetType="TextBlock" BasedOn="{StaticResource BasicTextBlock}">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="FontWeight" Value="Normal" />
            </Style>

            <Style x:Key="H3TextBlock" TargetType="TextBlock" BasedOn="{StaticResource BasicTextBlock}">
                <Setter Property="FontSize" Value="21" />
                <Setter Property="FontWeight" Value="SemiLight" />
            </Style>

            <Style x:Key="H2TextBlock" TargetType="TextBlock" BasedOn="{StaticResource BasicTextBlock}">
                <Setter Property="FontSize" Value="34" />
                <Setter Property="FontWeight" Value="Light" />
            </Style>

            <Style x:Key="H1TextBlock" TargetType="TextBlock" BasedOn="{StaticResource BasicTextBlock}">
                <Setter Property="FontSize" Value="55" />
                <Setter Property="FontWeight" Value="ExtraLight" />
            </Style>

            <Style x:Key="LabelTextBlock" TargetType="TextBlock" BasedOn="{StaticResource ParagraphTextBlock}" />


            <!-- buttons -->
            <Style x:Key="BasicButton" TargetType="Button" BasedOn="{StaticResource BasicControl}">
                <Setter Property="Margin" Value="0,10" />
            </Style>

            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BasicButton}">

            </Style>

            <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource BasicButton}">
                <Setter Property="Background" Value="Transparent" />
            </Style>

            <!-- edit boxes / pickers -->
            <Style x:Key="BasicSearchBox" TargetType="SearchBox" BasedOn="{StaticResource BasicControl}">
                <Setter Property="SearchHistoryEnabled" Value="True" />
            </Style>

            <Style x:Key="BasicTextBox" TargetType="TextBox" BasedOn="{StaticResource BasicControl}">
                <Setter Property="IsSpellCheckEnabled" Value="True" />
            </Style>

            <Style x:Key="BasicDatePicker" TargetType="DatePicker" BasedOn="{StaticResource BasicControl}">

            </Style>

            <Style x:Key="BasicTimePicker" TargetType="TimePicker" BasedOn="{StaticResource BasicControl}">

            </Style>

            <Style x:Key="BasicPasswordBox" TargetType="PasswordBox" BasedOn="{StaticResource BasicControl}">
                <Setter Property="PasswordRevealMode" Value="Peek" />
            </Style>

            <Style x:Key="BasicRadioButton" TargetType="RadioButton" BasedOn="{StaticResource BasicControl}"></Style>

            <!-- individual styles -->
            <!-- display entry -->
            <Style x:Key="DescriptionTextBlock" TargetType="TextBlock" BasedOn="{StaticResource LabelTextBlock}" />

            <Style x:Key="ValueTextBlock" TargetType="TextBlock" BasedOn="{StaticResource H3TextBlock}">
                <Setter Property="IsTextSelectionEnabled" Value="True" />
            </Style>

            <!-- edit entry -->
            <Style x:Key="ValueTextBox" TargetType="TextBox" BasedOn="{StaticResource BasicTextBox}" />

            <Style x:Key="ValueDatePicker" TargetType="DatePicker" BasedOn="{StaticResource BasicDatePicker}" />

            <Style x:Key="ValueTimePicker" TargetType="TimePicker" BasedOn="{StaticResource BasicTimePicker}" />

            <!-- navigation -->
            <Style x:Key="ItemHeader" TargetType="TextBlock" BasedOn="{StaticResource H2TextBlock}" />
            <Style x:Key="ItemDescription" TargetType="TextBlock" BasedOn="{StaticResource ParagraphTextBlock}" />

            <!-- login -->
            <Style x:Key="LoginPasswordBox" TargetType="PasswordBox" BasedOn="{StaticResource BasicPasswordBox}" />

            <!-- info -->
            <Style x:Key="QuestionTextBlock" TargetType="TextBlock" BasedOn="{StaticResource ParagraphTextBlock}" />
            <Style x:Key="AnswerTextBlock" TargetType="TextBlock" BasedOn="{StaticResource ParagraphTextBlock}">
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>

            <!-- welcome -->
            <Style x:Key="WelcomeRadioButton" TargetType="RadioButton" BasedOn="{StaticResource BasicRadioButton}" />

            <!-- search -->
            <Style x:Key="SearchBox" TargetType="SearchBox" BasedOn="{StaticResource BasicSearchBox}" />

            <!-- info -->
            <Style x:Key="TraceTextBlock" TargetType="TextBlock" BasedOn="{StaticResource ParagraphTextBlock}" />



        </ResourceDictionary>
    </Application.Resources>
</Application>
